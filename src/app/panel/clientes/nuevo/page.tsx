import { NewClient } from "@/core/components";

export default function ClientNewPage() {
  return (
    <div className="w-full h-full flex flex-col gap-3 justify-center items-center">
      <NewClient/>
    </div>
  );
}